# AAA fundamentals + first Vitest test, applied independently

Joy completed Lesson 0001's hands-on task without help: implemented `formatDuration` in
[app/_lib/formatDuration.js](../app/_lib/formatDuration.js) and wrote the two missing test
cases in `formatDuration.test.js` following the Arrange-Act-Assert shape shown in the lesson,
then merged it (commit `466bf16`). This confirms real command of `describe`/`test`/`expect`
and the `toBe` matcher, not just exposure — future lessons can build directly on AAA without
re-explaining it, and can move to matchers/concepts `toBe` doesn't cover (e.g. `toEqual` for
arrays/objects, since `toBe` only suits primitives).

**Evidence:** exercise completed unprompted, correct AAA structure in both new test cases,
correct expected values (`"00:05"`, `"01:05"`), work merged into the project rather than left
as a throwaway exercise.
